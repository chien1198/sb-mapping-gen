# Raw input documents may contain confidential bank data — do not commit
input/*
!input/.gitkeep

# extract/ and mapping/ are tracked (private repo) so table add/rename/
# remove and mapping changes are visible in git history.

.venv/
__pycache__/
*.pyc
.DS_Store
